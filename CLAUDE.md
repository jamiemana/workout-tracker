# Project Instructions

## Git

- Never include `Co-Authored-By` trailers in commit messages. The Vercel Hobby plan blocks deploys when it can't associate a committer with a GitHub user.
- Push changes directly to `main`. This is a single-user app; no PR / review flow is needed.

